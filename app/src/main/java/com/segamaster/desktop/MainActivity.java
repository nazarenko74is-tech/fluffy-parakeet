package com.segamaster.desktop;
import android.Manifest;import android.app.*;import android.content.*;import android.content.pm.PackageManager;import android.graphics.Color;import android.net.Uri;import android.os.*;import android.provider.Settings;import android.view.Gravity;import android.widget.*;
public class MainActivity extends Activity{int dp(int v){return(int)(v*getResources().getDisplayMetrics().density+.5f);} TextView t(String s,int z){TextView x=new TextView(this);x.setText(s);x.setTextColor(Color.WHITE);x.setTextSize(z);x.setPadding(dp(4),dp(8),dp(4),dp(8));return x;} public void onCreate(Bundle b){super.onCreate(b);LinearLayout r=new LinearLayout(this);r.setOrientation(LinearLayout.VERTICAL);r.setPadding(dp(20),dp(25),dp(20),dp(20));r.setBackgroundColor(Color.rgb(11,11,11));TextView h=t("SEGA MASTER",30);h.setTextColor(Color.rgb(212,175,55));h.setGravity(Gravity.CENTER);r.addView(h,new LinearLayout.LayoutParams(-1,dp(60)));TextView d=t("DEMO V1\nЖивой персонаж поверх экрана",18);d.setGravity(Gravity.CENTER);r.addView(d,new LinearLayout.LayoutParams(-1,dp(80)));Button p=new Button(this);p.setText("1. РАЗРЕШИТЬ ПОВЕРХ ЭКРАНА");p.setOnClickListener(v->overlay());r.addView(p,new LinearLayout.LayoutParams(-1,dp(58)));Button s=new Button(this);s.setText("2. ЗАПУСТИТЬ SEGA MASTER");s.setOnClickListener(v->start());r.addView(s,new LinearLayout.LayoutParams(-1,dp(58)));Button x=new Button(this);x.setText("УБРАТЬ SEGA MASTER");x.setOnClickListener(v->stopService(new Intent(this,SegaOverlayService.class)));r.addView(x,new LinearLayout.LayoutParams(-1,dp(58)));r.addView(t("Перетащи персонажа пальцем или нажми на него.\nСледующий этап: характер и фразы.",15),new LinearLayout.LayoutParams(-1,dp(100)));setContentView(r);}void overlay(){if(!Settings.canDrawOverlays(this))startActivity(new Intent(Settings.ACTION_MANAGE_OVERLAY_PERMISSION,Uri.parse("package:"+getPackageName())));else Toast.makeText(this,"Разрешение уже выдано",Toast.LENGTH_SHORT).show();}void start(){if(!Settings.canDrawOverlays(this)){overlay();return;}if(Build.VERSION.SDK_INT>=33&&checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS)!=PackageManager.PERMISSION_GRANTED)requestPermissions(new String[]{Manifest.permission.POST_NOTIFICATIONS},7);Intent i=new Intent(this,SegaOverlayService.class);if(Build.VERSION.SDK_INT>=26)startForegroundService(i);else startService(i);}}
